name: Deploy to GitHub Pages

on:
    workflow_call:

jobs:
    deploy_to_github_pages:
        runs-on: ubuntu-latest

        steps:
            - name: Deploy to GitHub Pages
              id: deployment
              uses: actions/deploy-pages@v4
